const assert = require('assert');
import ModuleFour from '../src/modules/ModuleFour';

describe('Coding practice problems: https://projecteuler.net/problem=4',() => {
  describe('\n A palindromic number reads the same both ways. The largest palindrome made from the product of two 2-digit numbers is 9009 = 91 × 99.', () => {
    it('Check to make sure that a palindrome is calculated correctly.', () => {
      console.log('NOT IMPLEMENTED');
    });
  });
});
