const utils = {
  // Takes the size of a number (2, 3), and interval number.
  // Does a double for loop i, and j both start at the highest numbers and count down.
  // The interval for both numbers is all numbers while keeping the numbers
  // furthest to the left the same. Multiply the 2 numbers and return the
  // result.
  generateAndMultiply: (number, interval) => {
    return null;
  },
  // Return true if number is palindrome otherwise false.
  isPalindrome: (number) => {
    return true;
  }
}

export default utils;
